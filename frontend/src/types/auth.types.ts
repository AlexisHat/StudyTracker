export type LoginData = {
    username: string;
    password: string;
  };
  
  export type RegisterData = {
    email: string;
    username: string;
    fullName: string;
    password: string;
  };
  